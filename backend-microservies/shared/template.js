const { processTemplate, validateTemplate, extractVariables, generateSampleData } = require('./templateService');

module.exports = {
  processTemplate,
  validateTemplate,
  extractVariables,
  generateSampleData,
};
